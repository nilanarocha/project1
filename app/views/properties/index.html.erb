<h1>Properties</h1>
<% @properties.each do |property| %>
  <%= link_to property, :class => 'style-item-link', :method => 'get' do %>
    <%= image_tag property.image, :class => 'style-item-img' if property.image.present? %>
    <%= property.address %>
  <% end %>
<% end %>
